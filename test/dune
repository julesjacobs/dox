(cram
 (deps
  %{bin:dox}
  block-boundaries.fixture
  late-call.fixture
  query.fixture
  tail-bytecode-exact.fixture
  tail-bytecode-over.fixture
  tail-bytecode-partial.fixture
  tail-bytecode-tupled.fixture
  tail-events.fixture
  tail-stack.fixture))
