# Complete call capture

    let rec burn n =
      if n = 0 then ()
      else burn (n - 1)

    let () = burn 400

    let late value =
      let doubled = value * 2 in
      doubled + 1

    let answer = late 20
