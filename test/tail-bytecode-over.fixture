    let rec loop remaining =
      let step () =
        if remaining = 0 then 0 else loop (remaining - 1) ()
      in
      step
    let result = loop 20 ()
    let () = print_endline (string_of_int result)
