# Demos

These pages use Dox as more than a code runner:

- [[Demos.Inference]] — a complete, inspectable Algorithm W.
- [[Demos.Tracing]] — a compact recursive execution trace.
- [[Demos.Visualization]] — an OCaml-generated SVG view.

Each demo is self-contained. Edit the inputs near the bottom of a page first;
the existing result will fade while the new program is compiled and evaluated.
